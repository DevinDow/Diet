<%= form_with(model: @meal, url: @meal.new_record? ? day_meals_path : @meal, local: true) do |form| %>
<%# form url: new Meal POSTs to /day/id/meal; update Meal PATCHes to /meal/id; both Controller methods then redirect to /day/id %>
  <% if meal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(meal.errors.count, "error") %> prohibited this meal from being saved:</h2>

      <ul>
      <% meal.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <h2><%= @day.pp %></h2>

  <div class="field">
    <%= form.label :time %>
    <%= form.time_select :time %>
  </div>

  <div class="field">
    <%= form.label :foods %>
    <%= form.text_field :foods %>
  </div>

  <div class="field">
    <%= form.label "Veggies" %>
    <%= form.number_field :categories, options = { multiple: true, style: "width: 50px" } %>
  </div>

  <div class="field">
    <%= form.label "Fruits" %>
    <%= form.number_field :categories, options = { multiple: true, style: "width: 50px" } %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
