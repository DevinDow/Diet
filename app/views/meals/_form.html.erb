<%= form_with(model: @meal, url: @meal.new_record? ? day_meals_path : @meal, local: true) do |form| %>
<%# form url: new Meal POSTs to /day/id/meal; update Meal PATCHes to /meal/id; both Controller methods then redirect to /day/id %>
  <% if meal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(meal.errors.count, "error") %> prohibited this meal from being saved:</h2>

      <ul>
      <% meal.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <h2><%= @day.pp %></h2>

  <table>
  <thead>
    <tr>
      <td><strong><%= form.label :time %></strong></td>
      <td><strong><%= form.label :foods %></strong></td>
      <td><strong><%= form.label "Veggies" %></strong></td>
      <td><strong><%= form.label "Fruits" %></strong></td>
      <td><strong><%= form.label "Proteins" %></strong></td>
      <td><strong><%= form.label "Carbs" %></strong></td>
      <td><strong><%= form.label "Fats" %></strong></td>
      <td><strong><%= form.label "Seeds" %></strong></td>
      <td><strong><%= form.label "Oils" %></strong></td>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><%= form.time_select :time %></td>
      <td><%= form.text_field :foods, options = { style: "width: 500px" } %></td>
      <td><%= form.number_field :categories, options = { multiple: true, style: "width: 50px" } %></td>
      <td><%= form.number_field :categories, options = { multiple: true, style: "width: 50px" } %></td>
      <td><%= form.number_field :categories, options = { multiple: true, style: "width: 50px" } %></td>
      <td><%= form.number_field :categories, options = { multiple: true, style: "width: 50px" } %></td>
      <td><%= form.number_field :categories, options = { multiple: true, style: "width: 50px" } %></td>
      <td><%= form.number_field :categories, options = { multiple: true, style: "width: 50px" } %></td>
      <td><%= form.number_field :categories, options = { multiple: true, style: "width: 50px" } %></td>
      <td><%= form.submit %></td>
    </tr>
  </tbody>
</table>

<% end %>
